public static class Common
{
    public static readonly int MaxPerObjectInGame = 60;
    
    //Editor is smaller because objects don't live as long when shifting the timeline
    public static readonly int MaxPerObjectInEditor = 54;
}
